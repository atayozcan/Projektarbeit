Ein HTTP-Server in Go, der die Standard-Bibliothek `net/http` verwendet.
Der Server lauscht auf Port 8080 und antwortet auf alle Anfragen mit "Hello from Go!".

## Server

```
=== HTTP Server (Go) ===

Server: Listening on port 8080
Server: Connection accepted.
Server: Request received, sending response...
Server: Connection closed.
```
